#include <ctype.h>

/**
 * check_digit - checks parameter is a number
 * char *parameter
 * 
 * Return: 1 for success, 0 for failure
 */

int check_digit(char *parameter)
{
	while (parameter)
	{
		if (isdigit(parameter[i] == 0)
			return (0);
		else
			i++;
	}
	return (1);
}